--SELECT * FROM STUDENTS

IF OBJECT_ID ('STUDENT_LIFE', 'U')IS NULL 
BEGIN
CREATE TABLE STUDENT_LIFE
(First_Name VARCHAR (100) NOT NULL,
Last_Name VARCHAR (100),
Admission_Number INT NOT NULL PRIMARY KEY,
Gender CHAR(1) CHECK(Gender IN ('M', 'F')),
Age INT CHECK (Age > 0),
Grade SMALLINT,
Is_Active BIT
);
END;

IF OBJECT_ID('STUDENT_LIFE', 'U') IS NULL 
BEGIN
  CREATE TABLE STUDENT_LIFE (
    First_Name VARCHAR(100) NOT NULL,
    Last_Name VARCHAR(100),
    Admission_Number INT NOT NULL PRIMARY KEY,
    Gender CHAR(1) CHECK(Gender IN ('M', 'F')),
    Age INT CHECK (Age > 0),
    Grade SMALLINT,
    Is_Active BIT
  );
END;

SELECT * FROM STUDENT_LIFE

ALTER TABLE STUDENT_LIFE DROP COLUMN Grade
ALTER TABLE STUDENT_LIFE 
ADD Registration_Number VARCHAR (100);

ALTER TABLE STUDENT_LIFE
ALTER COLUMN Is_Active VARCHAR(100) NOT NULL;


EXEC sp_rename 'STUDENT_LIFE.Active', 'Not_Active', 'COLUMN';


ALTER TABLE STUDENT_LIFE
ADD CONSTRAINT UNQ_STD UNIQUE (REGISTRATION_NUMBER);

ALTER TABLE STUDENT_LIFE
DROP  CONSTRAINT UNQ_STD

EXEC sp_rename 'STUDENT_LIFE','STUDENT_LOG';


SELECT * FROM STUDENT_LOG
